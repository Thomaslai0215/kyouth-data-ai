# Frontend talks to the backend from the browser, so use localhost (not the Docker service name).
BACKEND_URL=http://localhost:8001/chat

# Backend LLM settings
CHAT_MODEL=gemini-2.5-flash
GOOGLE_API_KEY=

# For local Ollama outside Docker
OLLAMA_BASE_URL=http://127.0.0.1:11434

